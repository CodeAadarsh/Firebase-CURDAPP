import {Dimensions} from 'react-native';
const {width, height} = Dimensions.get('window');

export const COLORS = {
    'PRIMARY' : '#ff0066',
    'SECONDORY' : '#FFF',
    'ACCENT' : '#bb004b'
}
export const SIZES ={
    width,
    height
}
